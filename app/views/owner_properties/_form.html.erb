<%= form_for([owner, owner_property]) do |f| %>
  <% if owner_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(owner_property.errors.count, "error") %> prohibited this owner_property from being saved:</h2>

      <ul>
      <% owner_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %>
    <%= f.text_field :key %>
  </div>

  <div class="field">
    <%= f.label :str_val %>
    <%= f.text_field :str_val %>
  </div>

  <div class="field">
    <%= f.label :num_val %>
    <%= f.text_field :num_val %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
