<%= form_for(auth_user) do |f| %>
  <% if auth_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auth_user.errors.count, "error") %> prohibited this auth_user from being saved:</h2>

      <ul>
      <% auth_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :userid %>
    <%= f.text_field :userid %>
  </div>

  <div class="field">
    <%= f.label :passwd %>
    <%= f.text_field :passwd %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :mail %>
    <%= f.text_field :mail %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_area :address %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
